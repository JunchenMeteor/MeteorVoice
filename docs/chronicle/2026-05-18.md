# 2026-05-18 Phase 1 Implementation

## Summary

Scaffolded and implemented Phase 1 of English Conversation Coach: auth, scenario selection, voice session lifecycle with mock providers, theme tokens, and LangGraph conversation workflow.

## Files Created

- Next.js 16 + TypeScript scaffold
- lib/supabase/client.ts, lib/supabase/server.ts
- lib/utils.ts - cn() className merging
- app/middleware.ts - auth guard
- app/globals.css - 6 themes via CSS tokens + shared utility classes
- components/ui/button.tsx, card.tsx, input.tsx
- components/Sidebar.tsx - collapsible with SVG icons
- components/ThemeProvider.tsx - theme context + localStorage
- app/layout.tsx - app shell
- app/login/page.tsx - Supabase Auth login/signup
- lib/scenarios.ts - 5 scenarios, 6 accent profiles
- lib/providers/types.ts - STT/TTS/AI interfaces
- lib/providers/mock-stt.ts, mock-tts.ts, mock-ai.ts
- lib/providers/ai-provider.ts - Vercel AI SDK + DeepSeek with mock fallback
- lib/conversation-workflow.ts - LangGraph state machine (7 states)
- app/page.tsx - scenario selection
- app/session/page.tsx + SessionPage.tsx - voice session UI
- app/history/page.tsx - mock history
- app/settings/page.tsx - theme switcher
- app/api/session/route.ts - session API
- supabase/migrations/001_init.sql - full schema + seed data
- .env.local.example

## Validation

- npm run build - passes
- npm run dev - needs Supabase env vars for login

## Deferred

- Real TTS provider verification (provider credentials needed)
- Real DeepSeek testing (API key needed)
- Supabase migrations applied in the target project

## Updated Status

- RLS migration added in `supabase/migrations/002_rls.sql`.
- TTS preference migration added in `supabase/migrations/003_tts_preferences.sql`.
- Username/phone login now follows the MeteorTest account parsing pattern.
- Domestic TTS adapters are scaffolded for Xunfei, Volcengine, and Tencent Cloud.
- Settings can switch TTS providers and disables unsupported accent choices.
- Real provider audio still requires external credentials and live provider verification.
